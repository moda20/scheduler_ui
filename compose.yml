version: '3.8'


services:
  scheduler_ui:
    build:
      context: .
      dockerfile: DOCKERFILE
      args:
        SCRAP_SERVER_ENDPOINT: ${SCRAP_SERVER_ENDPOINT}
    container_name: scheduler_ui
    env_file: .env
    restart: always
    ports:
      - "8096:80"
